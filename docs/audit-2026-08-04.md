# Audit complet — Sillage — 2026-08-04

**Méthode** : 8 subagents d'audit en parallèle (architecture, sécurité SQL, Edge Functions, secrets, qualité/tests, performance, design system, pipeline) + 3 subagents reviewer indépendants qui ont re-vérifié les constats sensibles (16 challengés, 0 faux positif, plusieurs sévérités recalibrées).
**Périmètre** : `app/`, `src/`, `supabase/` (58 migrations, 8 Edge Functions), `scripts/`, historique git.
**Outils de mesure** : `npx tsc --noEmit` (0 erreur), `npx jest --ci` (800/800 verts).

---

## 1. Résumé exécutif

Le projet est en **excellent état de santé** : 0 erreur TypeScript, 800 tests verts, 0 TODO/FIXME, 0 secret réel dans l'arbre versionné ni dans l'historique, 0 `fontWeight`, RLS actif sur toutes les tables, et une discipline rare sur les conventions maison. **Aucun constat CRITIQUE.** Le seul point **ÉLEVÉ** est une RPC `recompute_perf_strings` exécutable sans authentification qui pose un verrou exclusif sur la table `parfums` (fix = 1 ligne de migration). Quatre points **MOYEN** à traiter (purge destructive par défaut, dry-run qui pollue un checkpoint, deux cibles tactiles 40 px, Reduced Motion manquant sur ScanLoading). Le reste est du polish BAS/INFO.

---

## 2. Constats par sévérité

### ÉLEVÉ (1)

| # | Constat | Fichier:ligne | Reco |
|---|---|---|---|
| S1 | RPC `recompute_perf_strings()` SECURITY DEFINER sans garde d'identité, grant `service_role` mais **pas de REVOKE PUBLIC** → exécutable par la clé anon (embarquée dans l'APK). Le corps fait `ALTER TABLE parfums DISABLE TRIGGER` (verrou **ACCESS EXCLUSIVE** sur la table centrale) + UPDATE massif. Boucle d'appels anon = blocage des lectures catalogue. | `supabase/migrations/0058_perf_longevity_5_cranks.sql:387-433` | Nouvelle migration : `revoke all on function public.recompute_perf_strings() from public, anon;` (le grant service_role suffit au cron). Modèle déjà présent en `0006:282`. |

### MOYEN (5)

| # | Constat | Fichier:ligne | Reco |
|---|---|---|---|
| M1 | `purge-2x.ts` destructif **par défaut** (dry-run opt-in), supprime ~25 000 objets cloud sans confirmation. Incohérent avec `backfill-*` (dry-run par défaut). Atténué : invocation manuelle uniquement, régénérable. | `scripts/images/purge-2x.ts:27,44-45` | Inverser le défaut : dry-run par défaut + `--write` opt-in, ou confirmation interactive. |
| M2 | `import-fresh.ts` : le mode `--dry-run` pousse dans `progress.done` et sauve le checkpoint → le run réel suivant saute tout (« Rien à faire »). `--overwrite` ne contourne pas. Seul des 3 scripts sans protection. | `scripts/fragrantica/import-fresh.ts:396,408,420` | Wrapper push/save dans `if (!dryRun)` + ajouter un flag `--refresh`. |
| M3 | Cibles tactiles `settingsBtn` et `avatarBtn` 40×40 **sans hitSlop** (< 44 px, règle §6.2) — chrome permanent des 4 onglets. | `src/features/search/SearchChrome.tsx:293,296,360-370` | `hitSlop={{top:4,bottom:4,left:4,right:4}}` sur les deux Pressable. |
| M4 | `ScanLoading` : halo + particules en `withRepeat` **sans `useReducedMotion`** — pas coupés en Reduced Motion, alors que le guide §6.7 nomme explicitement ce composant. (`cancelAnimation` est bien présent.) | `src/features/scan/ScanLoading.tsx:43,51,86` | `useReducedMotion()` → halo statique + particules masquées si réduit. |
| M5 | Service `possessions` (CRUD métier réel) **sans aucun test** unitaire. | `src/services/possessions.ts` + `impl/possessions.supabase.ts` | Ajouter une suite de tests. |

### BAS (12)

| # | Constat | Fichier:ligne |
|---|---|---|
| B1 | `send-weather-notifications` : fetch des users éligibles **sans pagination** → tronqué à 1000 (db-max-rows), ordre non déterministe. Bug d'échelle latent (pas une fuite). | `supabase/functions/send-weather-notifications/index.ts:26-32` |
| B2 | `delete-user-account` : repli sur `iat` du JWT si `amr` absent — `iat` rafraîchi par rotation silencieuse. Chemin AMR primaire robuste (vérifié dans GoTrue), repli quasi mort mais fail-open. | `supabase/functions/delete-user-account/index.ts:33-39` |
| B3 | Clé Firebase **web** (`AIza…`, publique par design) commitée en v4.0, retirée v7.0, toujours dans l'historique. Risque uniquement si le projet `parfumscan-60549` est encore actif. | historique git (`1508257`) |
| B4 | `.gitignore` ne couvre pas `.env.production`/`.env.staging`/`.env.development` (seulement `.env` exact + `.env*.local`). Aucune variante sur disque actuellement. | `.gitignore:34-36` |
| B5 | `generate-note-images.ts` : `fetchUniqueNotes()` sans `.range()` → plafonné ~1000 lignes. Pipeline d'assets dormant (sortie non consommée par l'app). | `scripts/images/generate-note-images.ts:245-247` |
| B6 | `.env.example` : `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` absente (requise Android) → setup frais incomplet. Documentée dans README. | `.env.example` vs `app/_layout.tsx:31` |
| B7 | `test-supabase-e2e.ts` : user jetable créé en prod, cleanup pas dans un `try/finally` → user orphelin si crash. Écritures strictement scopées, zéro touch catalogue. | `scripts/test-supabase-e2e.ts:205-222` |
| B8 | Double haptique SOTD (picker + parent) et type `light` au lieu de `success` (§2.6). | `SOTDPicker.tsx:89-92` + `communaute.tsx:253` + `collection.tsx:337` |
| B9 | Onglet Communauté : `ScrollView` + 9 `.map()` non virtualisés. Contenu borné par des LIMIT SQL durs → BAS latent. | `app/(tabs)/communaute.tsx:339-709` |
| B10 | Vue Alertes (favoris) : `renderItem` arrow inline + styles inline → re-rendu des cellules (liste petite, déjà windowée). | `app/(tabs)/favoris.tsx:517-573` |
| B11 | `app/admin.tsx` : `await ImagePicker…` sans try/catch ni `.catch()`. | `app/admin.tsx:73` |
| B12 | Divers scripts : `purge-2x` reset DB même si Storage échoue ; `--limit` imprécis ; `readEnvVar` ne strippe pas les quotes ; pagination `.range()` sans `.order()` ; référence à un script mort (`export-firestore`). | `scripts/` (voir rapport subagent 8) |

### INFO (sélection)

- Cache météo : code = **10 min** (`weather.ts:3`), doc = 30 min (`reference.md`, AGENTS.md) → divergence code/doc à trancher.
- AGENTS.md annonce « 725 tests / 66 suites » ; réel **800 / 72** → doc décrochée.
- Migration `0014` absente = saut de numérotation inoffensif (jamais existée, zéro référence cassée).
- `export_user_data` (RGPD) omet `parfum_votes`, `follows`, `profiles`, `runner_scores`.
- JWT anon hardcodé `src/config/env.ts:16-17` = clé locale officielle supabase-cli, publique par design, gatée → aucun risque.
- `parfum_perf` conserve un paramètre `p_user_id` résiduel (écrasé serveur dès la 1ʳᵉ instruction).
- `search_parfums.max_results` / `seasonal_parfums.lim` non plafonnés (données publiques, conso réseau non bornée).
- `runner_scores.skin` sans contrainte de longueur ; pas de policy DELETE (opt-in assumé).
- Haptique sur auto-stop vocal (événement automatique) ; `#000` viseur caméra hors §2.3 ; `DockBar.tsx:383` opacité manuelle `+'E0'` hors helper `alpha()`.

---

## 3. Confirmations majeures (points sains)

- **Sécurité** : 27/28 fonctions DEFINER correctement gardées (auth.uid() forcé + garde propriété) ; aucune fuite de données privées dans les RPC publiques (double filtre `is_public`, notes perso jamais projetées) ; zéro vecteur d'injection SQL ; RLS owner sur toutes les tables user ; quotas atomiques non contournables ; `share` public = escapeHtml systématique + données publiques uniquement ; CASCADE RGPD complet.
- **Secrets** : 0 secret réel dans l'arbre versionné ni l'historique ; tous les scripts lisent `.env` via `readEnvVar` fail-fast ; proxy masqué dans logs et erreurs ; `config.toml` 100 % `env(...)`.
- **Qualité** : 0 erreur TS, 800/800 tests verts, 0 TODO, 0 log prod (4 `console.log` tous gatés `__DEV__`), `src/utils` testé à 100 %.
- **Performance** : virtualisation généralisée (y compris carousels), projections `CARD_COLUMNS`/`parfum_card`, caches à tous les étages (LRU + disque SWR + dédup inflight), Runner 100 % UI-thread, `ParfumCard` mémoïsé avec comparateur custom, zéro duplication realtime.
- **Design** : 0 `fontWeight`, 0 prix en `toFixed`, 0 couleur hardcodée hors exceptions, `cancelAnimation` sur toutes les feuilles animées.

---

## 4. Top 10 actions recommandées (effort / impact)

| # | Action | Sévérité | Effort | Impact |
|---|---|---|---|---|
| 1 | REVOKE PUBLIC sur `recompute_perf_strings` (1 ligne de migration) | ÉLEVÉ | Très faible | Fort |
| 2 | hitSlop sur settingsBtn/avatarBtn SearchChrome | MOYEN | Très faible | Moyen (a11y permanente) |
| 3 | `useReducedMotion` dans ScanLoading | MOYEN | Faible | Moyen (règle obligatoire) |
| 4 | purge-2x : dry-run par défaut + `--write` | MOYEN | Faible | Moyen (garde-fou prod) |
| 5 | import-fresh : guard `!dryRun` sur checkpoint | MOYEN | Faible | Moyen (bug silencieux) |
| 6 | Tests du service `possessions` | MOYEN | Moyen | Moyen (couverture) |
| 7 | Supprimer le projet Firebase `parfumscan-60549` (mitigation définitive B3) | BAS | Très faible | Faible→Moyen |
| 8 | `.gitignore` → `.env.*` + `!.env.example` | BAS | Très faible | Faible (hygiène) |
| 9 | Pagination `.range()`+`.order()` sur send-weather-notifications | BAS | Faible | Faible (échelle future) |
| 10 | Nettoyer haptique SOTD (1 seul, type success) + compléter `.env.example` | BAS | Très faible | Faible (polish) |

---

## 5. Comparaison avec l'audit précédent

Premier audit de cette série — pas de base de comparaison. Les rapports futurs pourront différer contre celui-ci (section prévue dans le prompt `docs/AUDIT-PROMPT.md`).

---

## 6. Correctifs appliqués le 2026-08-04 (même session)

| Constat | Correctif |
|---|---|
| S1 RPC anon | `supabase/migrations/0060_audit_hardening.sql` : `revoke all … from public, anon` sur `recompute_perf_strings` |
| Export RGPD partiel (INFO) | Migration 0060 : `export_user_data` v3.1.0 (+ perfVotes, following, followers, runnerScore, profile) |
| `runner_scores.skin` sans longueur (INFO) | Migration 0060 : CHECK `length(skin) <= 32` + troncature `left(..., 32)` dans `submit_runner_score` |
| M1 purge-2x | Dry-run PAR DÉFAUT + `--write` opt-in ; DB reset seulement si Storage remove OK ; `--limit` précis |
| M2 import-fresh | Checkpoint guardé par `!dryRun` + flag `--refresh` |
| M3 SearchChrome | `hitSlop` 4px sur settingsBtn/avatarBtn (→ 48 px effectifs) |
| M4 ScanLoading | `useReducedMotion` : particules masquées + halo statique |
| M5 possessions | `__tests__/services/possessions.test.ts` (11 tests) + bonus `theme-storage.test.ts` (6 tests) — 816 tests au total |
| B1 weather cron | Pagination `.range()` + `.order('user_id')` + check méthode POST |
| B2 réauth | Repli `iat` supprimé (fail-closed sans AMR) |
| B4 .gitignore | `.env.*` + `!.env.example` |
| B5 note-images | Pagination `.order('id')` + `.range()` par chunks de 1000 |
| B6 .env.example | + `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID`, `SUPABASE_LOCAL_SERVICE_ROLE`, `EXPO_PUBLIC_USE_SUPABASE_LOCAL`, DashScope |
| B7 e2e | Cleanup dans le handler de crash (`deleteTestUser` best-effort) |
| B8 haptique SOTD | 1 seul haptique, `hapticsSuccess()` dans le parent ; retiré du SOTDPicker |
| B10 favoris | `renderAlertItem` extrait en `useCallback` |
| B11 admin.tsx | try/catch sur `launchImageLibraryAsync` |
| B12 scripts | `readEnvVar` strip les quotes ; `.order('id')` sur paginations diff-brands/backfill-breakouts/import-fresh ; message erreur import-supabase corrigé ; quota analyze-perfume-image distingue 429/500 ; send-notification valide title/body/data ; check-price-alerts POST-only + order |
| INFO haptiques | « Retirer » → `hapticsError()` (StatuerSheet/FavoriSheet) ; auto-stop vocal sans haptique |
| INFO DockBar | Opacité via `alpha()` au lieu de `+'E0'` |
| INFO docs | AGENTS.md (816 tests, GOOGLE_WEB_CLIENT_ID, purge-2x), rules.md (816 tests, cache météo 10 min), reference.md (cache 10 min) |

**Non corrigé (décisions documentées)** :
- Plafonds `search_parfums.max_results` / `seasonal_parfums.lim` / `family_overviews` : recréation des fonctions jugée trop risquée sans instance Postgres locale de test (Docker Desktop éteint). Mitigation existante : `db-max-rows` PostgREST borne chaque réponse ; données publiques. À traiter quand `supabase start` est disponible.
- B3 clé Firebase historique : action externe — supprimer le projet `parfumscan-60549` dans la console Google Cloud.
- B9 Communauté non virtualisée : latent, bornes SQL dures — à migrer en FlatList seulement si les LIMIT sont levés.
- Signature `parfum_perf(p_user_id)` résiduelle : cosmétique, breaking pour le typage client.

**Déploiement requis** (à faire manuellement) :
```bash
supabase db push                                        # migration 0060
supabase functions deploy send-weather-notifications
supabase functions deploy delete-user-account
supabase functions deploy send-notification
supabase functions deploy check-price-alerts
supabase functions deploy analyze-perfume-image
```
